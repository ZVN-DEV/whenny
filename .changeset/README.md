# Changesets

This project uses [changesets](https://github.com/changesets/changesets) for version management.

To add a changeset: `npx changeset`
To version packages: `npm run version-packages`
To publish: `npm run release`
